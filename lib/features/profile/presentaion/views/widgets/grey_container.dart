import 'package:flutter/material.dart';

class GeryContainer extends StatelessWidget {
  const GeryContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 5,
      color: Colors.grey.withOpacity(.3),
    );
  }
}
