class ProductModel {
  final String name, description, image;
  final double price;

  const ProductModel({
    required this.name,
    required this.description,
    required this.image,
    required this.price,
  });
}
